import { TimeTransPipe } from './time-trans.pipe';

describe('TimeTransPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeTransPipe();
    expect(pipe).toBeTruthy();
  });
});
